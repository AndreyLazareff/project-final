<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- ========================= -->
    <!-- Properties                -->
    <!-- ========================= -->

    <property name="LOG_PATH" value="logs"/>
    <property name="LOG_FILE" value="task-manager"/>

    <property name="LOG_PATTERN"
              value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{40} - %msg%n"/>

    <!-- ========================= -->
    <!-- Console Appender          -->
    <!-- ========================= -->

    <appender name="CONSOLE"
              class="ch.qos.logback.core.ConsoleAppender">

        <encoder>
            <charset>UTF-8</charset>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>

    </appender>

    <!-- ========================= -->
    <!-- File Appender             -->
    <!-- ========================= -->

    <appender name="FILE"
              class="ch.qos.logback.core.rolling.RollingFileAppender">

        <file>${LOG_PATH}/${LOG_FILE}.log</file>

        <rollingPolicy
                class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">

            <fileNamePattern>
                ${LOG_PATH}/${LOG_FILE}.%d{yyyy-MM-dd}.%i.log
            </fileNamePattern>

            <!-- Максимальный размер одного файла -->
            <maxFileSize>10MB</maxFileSize>

            <!-- Хранить логи 30 дней -->
            <maxHistory>30</maxHistory>

            <!-- Максимальный общий объем логов -->
            <totalSizeCap>500MB</totalSizeCap>

            <cleanHistoryOnStart>true</cleanHistoryOnStart>

        </rollingPolicy>

        <encoder>
            <charset>UTF-8</charset>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>

    </appender>

    <!-- ========================= -->
    <!-- Logging Levels            -->
    <!-- ========================= -->

    <!-- Hibernate -->
    <logger name="org.hibernate.SQL" level="OFF"/>
    <logger name="org.hibernate.type.descriptor.sql" level="OFF"/>
    <logger name="org.hibernate.orm.jdbc.bind" level="OFF"/>

    <!-- Spring Security -->
    <logger name="org.springframework.security" level="WARN"/>
    <logger name="org.springframework.security.web" level="WARN"/>
    <logger name="org.springframework.security.config" level="WARN"/>

    <!-- Liquibase -->
    <logger name="liquibase" level="INFO"/>

    <!-- Логи приложения -->
    <logger name="com.lazareff.taskmanager" level="INFO"/>

    <!-- ========================= -->
    <!-- Root Logger               -->
    <!-- ========================= -->

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>

</configuration>